import { describe, it } from "vitest";

describe("S-07 study-session-flow integration skeleton", () => {
	it.todo("IT-AC01: startStudySession は未認証時に /login へ遷移する");
	it.todo("IT-AC03: active session があれば新規作成せず再利用する");
	it.todo("IT-AC07: progress.total は retry で増加しない");
	it.todo("IT-AC12: revealed=true 離脱後は裏面から再開する");
	it.todo("IT-AC19: rateCard は nextCard を同一レスポンスで返す");
});
